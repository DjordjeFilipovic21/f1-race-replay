{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:f1-cache-replay:schema:replay-data:v2:weather-sidecar",
  "title": "Browser delivery v2 weather sidecar",
  "description": "Optional native-cadence weather observations for a v2 browser replay. Arrays are aligned, and timeMs is strictly ascending.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contractVersion",
    "fixtureId",
    "timeMs",
    "airTempC",
    "humidityPct",
    "pressureMbar",
    "rainfall",
    "trackTempC",
    "windDirectionDeg",
    "windSpeedMps"
  ],
  "$comment": "Equal-length column arrays and strictly ascending timeMs are mandatory semantic checks enforced by the delivery model and loader; JSON Schema enforces non-negative unique timestamps and per-array ranges only. Measurements are nullable and fail closed; sentinel-prone FastF1 zeros are sanitized to null at the build boundary per the zero-sentinel policy. The v1 weather schema remains a frozen compatibility reference only; a v2 manifest and v2 fixtures must never reference it.",
  "$defs": {
    "fixtureId": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "sessionTimeMs": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9223372036854775807
    },
    "nullableFiniteTemperatureC": {
      "type": [
        "number",
        "null"
      ],
      "exclusiveMinimum": 0
    },
    "nullableHumidityPct": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0,
      "maximum": 100
    },
    "nullablePressureMbar": {
      "type": [
        "number",
        "null"
      ],
      "exclusiveMinimum": 0
    },
    "nullableRainfall": {
      "type": [
        "boolean",
        "null"
      ]
    },
    "nullableWindDirectionDeg": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0,
      "maximum": 359
    },
    "nullableWindSpeedMps": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0
    },
    "reference": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "schemaId",
        "sha256"
      ],
      "properties": {
        "path": {
          "const": "weather-sidecar.json"
        },
        "schemaId": {
          "const": "urn:f1-cache-replay:schema:replay-data:v2:weather-sidecar"
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    }
  },
  "properties": {
    "contractVersion": {
      "const": "v2"
    },
    "fixtureId": {
      "$ref": "#/$defs/fixtureId"
    },
    "timeMs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/sessionTimeMs"
      },
      "description": "Strictly ascending absolute session times. JSON Schema enforces uniqueness and non-negative values; ordering and alignment are semantic contract checks."
    },
    "airTempC": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableFiniteTemperatureC"
      }
    },
    "humidityPct": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableHumidityPct"
      }
    },
    "pressureMbar": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullablePressureMbar"
      }
    },
    "rainfall": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableRainfall"
      }
    },
    "trackTempC": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableFiniteTemperatureC"
      }
    },
    "windDirectionDeg": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableWindDirectionDeg"
      }
    },
    "windSpeedMps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/nullableWindSpeedMps"
      }
    }
  }
}
